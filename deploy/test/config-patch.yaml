apiVersion: apps/v1
kind: Deployment
metadata:
  name: mongodb-query-exporter
spec:
  template:
    spec:
      containers:
      - name: exporter
        volumeMounts:
        - name: config
          mountPath: /etc/mongodb_query_exporter/config.yaml
          subPath: config.yaml
      volumes:
      - name: config
        configMap:
          name: mongodb-query-exporter
